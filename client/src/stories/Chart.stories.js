import React from "react";

import { Chart } from "../components/Chart.js";

export default {
  title: "Chart",
  component: Chart,
};

const Template = () => <Chart />;

export const ForecastBarChart = Template.bind({});
