import React from "react";
import ZipButton from "../components/ZipButton.js";

export default {
  title: "Button",
  component: ZipButton,
};

const Template = () => <ZipButton>hallo</ZipButton>;

export const ZipChangeButton = Template.bind({});
