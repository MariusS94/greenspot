import React from "react";

import { ForecastCarousel } from "../components/ForecastCarousel.js";

export default {
  title: "Carousel",
  component: ForecastCarousel,
};

const Template = () => <ForecastCarousel />;

export const EnergyCarousel = Template.bind({});
